# AgentOS
Agent-OS is a lightweight workspace that allows users to design AI workflows using simple step-based blocks. Instead of relying on isolated prompts or complex frameworks, users can create structured pipelines where each step builds on the previous one.
